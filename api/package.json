{
  "name": "scraper-firebase-extension-api",
  "version": "1.0.0",
  "description": "API for scraper-firebase-extension",
  "main": "index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run clean && npm run compile",
    "build:watch": "npm run clean && tsc --watch",
    "clean": "rimraf lib",
    "compile": "tsc",
    "test": "jest"
  },
  "author": "Corie Watson",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^13.2.0",
    "typescript": "^5.8.2"
  },
  "devDependencies": {
    "@types/jest": "29.5.0",
    "jest": "29.5.0",
    "ts-jest": "29.1.2"
  }
}
